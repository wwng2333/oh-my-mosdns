log:
  level: info
  file: "/tmp/mosdns.log"

plugins:
  - tag: forward_alidns 
    type: forward
    args:
      upstreams:
        - addr: https://dns.alidns.com/dns-query
          bootstrap: 223.5.5.5

  - type: udp_server
    args:
      entry: forward_alidns
      listen: 127.0.0.1:5533
